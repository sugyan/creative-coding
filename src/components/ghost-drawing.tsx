import { ReactP5Wrapper } from "react-p5-wrapper";

import sketch from "../sketches/ghost-drawing";

const GhostDrawing = () => {
  return <ReactP5Wrapper sketch={sketch} />;
};
export default GhostDrawing;
